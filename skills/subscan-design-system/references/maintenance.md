# Maintenance

Resolve this package from its loaded SKILL.md and authoring/install mappings from the environment. If multiple copies exist, compare and reconcile them before editing; preserve scoped later decisions through an explicit reconciliation. Sync only intended changes and verify parity. Use the designated package in place when there is no mapped second copy.

Keep source facts, correctness constraints, candidate compositions and accepted visual decisions distinct. For evolving decisions record scope, status, last confirmation or verification date when known, and any superseded rule. A maintenance date is not an acceptance date. Prefer applicable, accepted and verified evidence over recency; retain unknown history explicitly.

The source map may record a revision as evidence provenance, not as a required Git checkout or permanent design authority. Exact audit measurements, PR issue counts, debug incidents and progress belong to task evidence. Preserve reusable failure mechanisms without requiring old conversations or reports.

Validate frontmatter, internal links, changed source pointers and mapped-copy parity. An environment-provided validator is optional; this package requires no particular Python environment. For substantial changes, reason through small-module repair, new-page composition, chain-specific account additions and non-explorer routing. Distinguish this review from independent behavioral testing and actual browser acceptance.

## Writing useful guidance

Lead ordinary design rules with the action, its applicable context and the intended result. Consolidate repeated rules at their owning reference and keep diagnostic examples optional. Retain explicit prohibitions for consequential safety or permission boundaries; state ordinary presentation choices through the preferred implementation and meaningful exceptions. Preserve confirmed intent when rewording, and assess changes by decision quality rather than keyword counts.
