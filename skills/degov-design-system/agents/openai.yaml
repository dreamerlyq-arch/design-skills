interface:
  display_name: "DeGov Design System"
  short_description: "Apply DeGov product design rules consistently"
  default_prompt: "Use $degov-design-system to add or refine this DeGov product UI without changing product behavior."
